# ascii-art

画像をアスキーアートに変換するツールです（macOS専用）

## 使い方

1. `ascii-night.command` をダウンロード
2. ダブルクリックで起動
3. 画像ファイルをターミナルにドラッグ＆ドロップしてEnter

初回起動時は自動でセットアップが行われます（パスワードの入力が必要です）

## 動作環境

- macOS（Intel / Apple Silicon どちらでも可）
